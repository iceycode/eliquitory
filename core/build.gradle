apply plugin: "java"

sourceCompatibility = 1.8 //set to whatever jdk to be used
[compileJava, compileTestJava]*.options*.encoding = 'UTF-8'

sourceSets.main.java.srcDirs = [ "src/" ]


eclipse.project {
    name = appName + "-core"
}
